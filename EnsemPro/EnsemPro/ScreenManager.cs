using Microsoft.Xna.Framework;

// Saving for later. Do not delete me.
namespace EnsemPro
{
    class ScreenManager : GameComponent
    {
        public ScreenManager(Game g)
            : base(g)
        {
        }

        public override void Update(GameTime gameTime)
        {
            base.Update(gameTime);
        }
    }
}
